# Python-Final-Project Notes
I created two projects, the one that marked local data_ipynb_ipynb(local data final version)is the one that need to upload the data,and the one that marked onlinedata could run the code directly without uploading the data. 
I also uploaded the cvs file to the repository. 
